<% @songs.each do |song|%>
<% @artist = Artist.find(song.artist_id)%>
<%= link_to "#{song.title} - #{@artist.name}", artist_path(artist) %>
<%= link_to @artist.name, artist_path(@artist) %>
<% end %>
